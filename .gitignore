# 진행기록, 백로그, ADR은 저장소 정본이라 추적한다.
# 로컬 전용 자료만 `docs/local/`에 두고 무시한다.
docs/local/

*.local
.env
.env.*
!.env.example
*.key
*.pem

node_modules/
dist/
.DS_Store

.worktrees/

# 로컬 전용 AI 인수인계 자료는 민감한 맥락을 담을 수 있어 공유하지 않는다.
.ai/local/
