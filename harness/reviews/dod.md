# dod 계약

완료·커밋·인수인계·PR 직전에 변경 전체를 검수한다. 자동 수정하지 않으며 `WARN`을 진행 허가로 바꾸지 않는다.

기준선 diff, committed, staged, unstaged, untracked를 합쳐 프로토콜·데이터·설정·구현·문서·운영 기록으로 분류한다. `PASS`, `FAIL`, `WARN`, `N/A`와 파일:줄 또는 문서 절 근거를 보고한다. 비밀정보, 정본·계약, 데이터·권한, 재현성, 용어·링크, 운영 기록, `git diff --check` 및 관련 테스트를 점검한다.
